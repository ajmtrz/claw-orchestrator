# Plan — Autoloop trust recovery R1

## Goal
Audit the six original incidents against clean upstream and the starting candidate, correct demonstrated defects through three causal RED→GREEN slices, and establish independently reviewable local completion using observed behavior and immutable evidence. No scalar; acceptance is binary gates.

## Authority and starting state
- Logical run: CLAWO-AUTOLOOP-DURABLE-RECOVERY-TRUST-RECOVERY-R1. Project P: /home/openclaw/workspace/projects/claw-orchestrator-autoloop-durable-recovery. Branch: fix/autoloop-durable-recovery. R means P/tasks/CLAWO-AUTOLOOP-DURABLE-RECOVERY-TRUST-RECOVERY-R1, controller-owned and read-only to Coder. A means project-relative .artifacts/CLAWO-AUTOLOOP-DURABLE-RECOVERY-TRUST-RECOVERY-R1.
- Starting commit C0 = 2694c0babcf16030278e58d829a7c71bcaa0f7a2; tree = d8ba3273c21ccaa54bab984f2a2f478d90cc6aed. Upstream U = 3e09b032a2f95fa520648f959f4ac9cdc7393350. Planning inspection confirmed these identities, clean worktree/index, and U ancestry; no merge is required. The dedicated plan/goal commits may advance HEAD while leaving implementation bytes unchanged.
- Preserve Tasks 1–8 commits, parent CLAWO-AUTOLOOP-DURABLE-RECOVERY-20260905-BOOTSTRAP, and superseded CLAWO-AUTOLOOP-DURABLE-RECOVERY-POST-UPSTREAM-TASK8-9-R2. Never resume R2 or create a competing logical run.
- Reuse immutable blobs: global AGENTS f34e0b83c389f6af6bbb11390b2edaa730bac501; RTK 7ae285e1a86e0ae00b3918e876f2c8b3ed2509bd; council 8e6df54447e1e9941f9aa8da38174b268ff7ff48. Council SHA-256 must equal e59b9224db28e84932501469f495ef34d38789c1be99e958ccc4b859e23a16cc. Compare the other hashes against R/preflight.json.
- Planner, sole Coder, and each fresh read-only Reviewer are exactly codex/gpt-6-astra. Use the six existing flat engine/model fields. Do not introduce UAB, AgentBinding, effort parameters, alternate defaults, substitutions, or configuration migration. Adapter test targets are separate from these worker identities.
- This turn writes only plan.md and goal.json through dedicated controls. No workers start in this turn. Subsequent execution uses one Coder and fresh Reviewer cycles under the approved run; Planner never implements or runs evaluations.

## Scope
- In: falsifiable incident reproduction, legacy tombstone diagnosis/correction, durable delivery and recovery, strict public success semantics, independent Reviewer recovery, authentic adapter tests, shared-surface non-regression, final verification/review, atomic local commits.
- Out: installation, dependency updates, push, PR, issue, release, deployment, Gateway operations, governance/configuration changes, customer-run recovery, Task 10, and publication work from the historical design. The current owner request supersedes those historical rollout/publication steps and older Reviewer bindings.
- Stash 951c1ec047ffad3c316f143dafb5acc2bf7f1343 and untracked tree 6b6c66ad331b4cfd58a08435e3691320acecfd8e remain untouched. Its six files and 96 declared PASS rows are negative process evidence only: never restore, copy, execute, or reuse them as tests, fixtures, or acceptance evidence.

## Constraints and exact fences
- All paths below are relative to P. Braces enumerate exact filenames; they are not wildcard authorization. Every production edit requires a failing behavioral test and a recorded causal explanation. Changes elsewhere require a concrete Planner fence amendment before implementation.
- Common verification fence: scripts/autoloop-trust-recovery/{collect.mjs,verify.mjs,verify.test.mjs,evidence.schema.json,live-boundary.ts}; src/__tests__/helpers/autoloop-trust-recovery.ts; src/__tests__/autoloop-trust-recovery-incidents.test.ts. New fixtures: src/__tests__/fixtures/autoloop-trust-recovery/{codex.mjs,claude.mjs,agy.mjs,cursor.mjs,README.md}.
- All Coder-owned generated evidence/output belongs only under A/evidence/<subject>/<attempt>/ and A/reports/. All of project-relative tasks/, including tasks/<run>/, is controller-owned and read-only to Coder; this plan grants no exception. The controller may later hash/reference Coder artifacts in its own ledger, but Coder never writes there. Controller-owned preflight, decisions, iteration controls, review verdicts, and all prior-run files remain immutable to Coder. plan.md and goal.json remain Planner-owned.
- Do not edit AGENTS.md, RTK.md, council.yaml, configs/, .github/, package.json, lockfiles, tsconfigs, vitest.config.ts, openclaw.plugin.json, credentials, installed runtimes, or unrelated product files. Do not amend, rebase, reset history, clean stashes, or broadly stage ignored files.
- Use existing installed dependencies and rtk-prefixed commands. No dependency-fetching invocation. Isolate registry, PID, run-store, subprocess and adapter scratch state before constructing SessionManager; its registry is rooted at os.homedir() (src/session-manager.ts:42). Use test-boundary path isolation with real persistence underneath; never repurpose HOME or CODEX_HOME, mutate the actual user registry, or run orphan cleanup against real sessions. If safe isolation is unavailable, record the affected capability as BLOCKED.

## Approach (Coder hint)
First establish a trustworthy observation harness and reproduce the claims against immutable subjects. Correct reservation lifetime, delivery identity, and public acceptance boundaries in that order, using existing implementation where it withstands falsification. Keep routine loops limited to the affected slice; run broad and live checks only after freezing the complete candidate.

## Preparation — evidence harness and baseline audit
- Read R/preflight.json, the R2 supersession checkpoint, the original design/implementation plan, and relevant history. Record hashes of protected evidence and stash objects. Use disposable detached checkouts under P/.worktrees/trust-recovery-r1/{upstream,start} at U and C0; do not switch or alter the active branch. Preserve clean tracked source bytes in both checkouts.
- Implement the common verification fence test-first, without restoring the rejected matrix. Use a small collector plus a read-only validator, not a replacement orchestration system. Run rtk proxy node --test scripts/autoloop-trust-recovery/verify.test.mjs. Negative cases must reject missing artifacts, fabricated invariant booleans, changed hashes, wrong subjects, missing fault observations, reordered evidence, incomplete repetitions, hidden failures, and Coder-authored review claims.
- Collector interface: rtk proxy node scripts/autoloop-trust-recovery/collect.mjs <group>. Groups are baselines, legacy, delivery, boundaries, adapters, focused, format, lint, build, typecheck, full, coverage, e2e, concurrency, nonregression, live. It uses fixed command definitions below, captures raw process results, and never executes commands taken from evidence records. Heavy groups require a frozen candidate manifest. Collector output and validator input defaults use A/evidence/ and A/reports/; no generated output may fall back to tasks/.
- baselines runs the six-incident harness against U and C0 with identical installed dependency/tool hashes and scenario inputs. Capture actual imported source paths/hashes so an upstream test cannot accidentally execute candidate code. Version-specific harness glue may translate existing interfaces; it must not backport fixes or treat a missing API as a passing reproduction.
- Preserve the reported 193/193 recovery/outbox result as prior evidence, then rerun those actual suites. Preserve the reported isolated tombstone failure and reproduce its exact title before correcting it. Existing green behavior needs verification and sensitivity evidence, not artificial production changes to manufacture RED.
- Commit the harness and audited baseline machinery atomically after its own tests pass. Record baseline observations separately; completing this preparation does not imply any incident was corrected.

## Canonical evidence schema
- Version 1 bundles contain run_id, case_id, requirement_ids, subject_kind (upstream/start/candidate/sensitivity), base/head/tree, tracked-input manifest SHA-256, harness/dependency/tool hashes, source import paths, and command execution IDs. RED with uncommitted tests additionally records the exact patch and test bytes; it cannot masquerade as final-candidate evidence.
- Each execution records actual argv, cwd, start/end, exit code or terminating signal, timeout, discovered/executed/skipped test IDs, and raw stdout/stderr/report paths with SHA-256. Nonzero exits, unhandled errors, cancellation, zero selected tests, and required skips cannot become PASS. Redact secrets at collection; preserve all non-secret diagnostic content and identify any redaction.
- Each behavioral observation references an actual artifact/event: contained relative path, hash, byte range or JSON pointer, event sequence, process/observer identity, and parsed value. Include registry/ledger snapshots, actual recipient bytes, process exits, barrier receipts, source checkpoint identity, and public responses as applicable.
- Assertions specify a fixed predicate and referenced observations, such as digest equality, ordered durable events, exact row count, absence across a complete event interval, or byte-preserving prefix comparison. Validator recomputes values from referenced bytes. Self-declared invariant flags and status strings are not proof; an observed API boolean is acceptable only when sourced to its response and checked against its durable postcondition.
- Crash cases require a witnessed barrier reached by the real code path, process loss at that boundary, and cold reconstruction from retained files. Test fixtures may emulate external CLI protocols and expose barriers; they must not generate the expected durable ledger, ACK, recovery receipt, or invariant result on behalf of production code.
- Each corrected behavior names the production defect it detects and supplies RED→GREEN evidence or a narrowly scoped sensitivity run on a disposable copy. Mutant/sensitivity artifacts are distinctly labeled and never counted as candidate gates. Reviewer independently checks tests, collector, predicates and raw observations; hashes alone cannot prove a fabricated event occurred.
- Evidence is append-only by subject/attempt. Missing, stale, ambiguous or unsupported evidence fails validation. Accepted exemptions retain their reasoning and Reviewer provenance; no denominator shrinking or synthetic replacement rows.

## Six-incident matrix — upstream, starting candidate, final candidate
- For each A–F record the same triggering inputs, expected observable outcome, U result, C0 result, final C result, source/history locators, causal slice, exact test IDs/commands, and evidence references. U/C0 reproduction disposition is REPRODUCED, NOT_PROBABLE, NOT_APPLICABLE, or NOT_PROVEN. Final incident classification is CORRECTED, FAILS, or NOT_PROVEN.
- CORRECTED requires a demonstrated failing behavior and verified remedy, including an identified historical fix when already present. FAILS means the final requirement is violated. NOT_PROBABLE/NOT_APPLICABLE requires positive technical exclusion evidence and independent review for each affected subject; inability to reproduce, absent tooling, or a missing public operation is insufficient. Such an accepted exclusion may satisfy the historical reproduction obligation without relabeling the incident CORRECTED. Unexplained NOT_PROVEN blocks completion; all applicable final behavioral requirements remain mandatory.
- A — Wrong directive replay: confirm directive A, persist its identity, introduce distinct directive B, interrupt the dispatch window, then resume/recover. Observe exact recipient bytes and hashes, delivery ID, idempotency key and ACK; A must replay when owed, B must not replace it, and acknowledged replay must cause no new effect. Exercise Planner→Runner→Dispatcher, not merely an outbox helper.
- B — Expired activity lease pauses recoverable work: persist a directive or complete Coder artifacts, lose the owning process, cross the lease boundary, then use supported resume/recovery. Observe reconstructed phase and the next safe action. Matching live ownership survives; absent ownership permits fenced recovery; unknown ownership blocks. Activity-watchdog expiry alone is not proof of process absence.
- C — Dead session name blocks recreation: leave a reservation after witnessed process death, recreate through the production path, and observe one orphan/release sequence and the next valid generation. Race a second manager; stale/live/unknown owners must not acquire or release the replacement.
- D — Reset reports success without replacement: exercise eagerRestart=true and observe old stop/absence, durable release and actual replacement generation/session. Inject replacement failure and occupied/unknown ownership; success must not be reported. Preserve the legacy boolean wrapper and the documented lazy-reset contract, whose postcondition is reusable state rather than an invented eager session (src/session-manager.ts:6489).
- E — Empty/no-effect chat succeeds: supply empty/whitespace, denied required tool, missing physical session, malformed control, and valid claimed control whose persistence fails. Observe typed failure, no unsupported phase advance and retained retry context. Exercise MCP and embedded HTTP: HTTP 202 is queue acceptance, followed by an observed terminal outcome. A conversational reply that claims no control must remain compatible.
- F — Existing checkpoint cannot receive Reviewer-only work: create an actual Git checkpoint and source iteration artifacts, stop all physical agents, recover and request only Reviewer. Observe persisted request/digest, matching recipient ACK and exactly one source-bound verdict; capture zero Coder starts and zero continuation runs. Duplicate/concurrent requests must not duplicate logical review effects; retain spawn_subagents compatibility.

## Slice 1 — Legacy lifetime, reservation and release
- Production fence: src/session-manager.ts; src/autoloop/{dispatcher.ts,types.ts}; src/kernel/file-lock.ts only if a separate reproduction proves the lock is causal. Test fence: src/__tests__/{session-manager.test.ts,session-manager-pidfile.test.ts,autoloop-dispatcher.test.ts,autoloop-trust-recovery-legacy.test.ts}, plus the common harness.
- RED: run rtk proxy npm test -- src/__tests__/session-manager.test.ts -t 'keeps a legacy generation-zero tombstone fenced until release evidence is durable'. Trace fixture→registry persistence→loader→claim→hooks→completion. The concrete hypothesis is that the fixed September 5 fixture expires under the seven-day loader TTL before release starts (src/constants.ts:52; src/session-manager.ts:237; src/__tests__/session-manager.test.ts:1167). Record whether hooks were actually reached; do not assume a release race from the title alone.
- Add controlled-clock cases below, exactly at, and above TTL; distinguish ordinary resumable metadata from established durable fences. Add real-file legacy registry cases with missing generation/session fields and explicitly undefined legacy identity. A fixture-clock defect may be corrected as such only after demonstrating it; production changes require a separate failing contract test. Never disable ordinary TTL globally or merely refresh a date to hide the issue.
- Mandatory competing-reservation regression: persist the generation-zero pending fence, enter the real release-evidence persistence boundary, attempt generation-one reservation from a second manager/process, and observe refusal plus unchanged ownership. After release evidence is durably observed, the rightful release completes and exactly one reservation succeeds. Repeat with process death before/after the evidence barrier and cold retry; record ledger rows and registry bytes, not callback booleans.
- Cover wrong generation/owner/session, live and unknown owners, absent owner takeover, partial persistence, shutdown, stale registry snapshots, post-release idempotency, and ordinary SessionManager TTL/name behavior. Acceptance: A–F-related legacy state is reconstructed conservatively; pending release remains fenced across reload/crash; no premature reuse or repeated evidence effect.
- GREEN command: rtk proxy npm test -- src/__tests__/session-manager.test.ts src/__tests__/session-manager-pidfile.test.ts src/__tests__/autoloop-dispatcher.test.ts src/__tests__/autoloop-trust-recovery-legacy.test.ts. Commit only the minimal accepted lifetime/release correction and regressions; obtain fresh read-only slice review once these gates pass.

## Slice 2 — Durable identity, crash recovery and resume
- Production fence: src/autoloop/{outbox.ts,secure-ledger.ts,recovery.ts,runner.ts,dispatcher.ts,messages.ts,types.ts}; src/session-manager.ts. Conditional causal dependencies: src/kernel/{file-lock.ts,engine.ts,store.ts}. Test fence: src/__tests__/{autoloop-outbox.test.ts,autoloop-secure-ledger.test.ts,autoloop-recovery.test.ts,autoloop-runner.test.ts,autoloop-dispatcher.test.ts,session-manager.test.ts,autoloop-durable-recovery-e2e.test.ts,autoloop-trust-recovery-delivery.test.ts}; src/__tests__/kernel/{engine.test.ts,store.test.ts}; common harness.
- RED: implement A/B and delivery portions of F through actual runner/dispatcher/persistence. Kill a subprocess after durable intent but before send, after recipient capture but before ACK, and after durable ACK but before phase consumption. Cold-start without warm maps. Also inject failed/torn persistence, digest/target mismatch, generation replacement, duplicate calls, stale recovery tokens, and uncertain prepared effects.
- Observe complete intent and digest before transport; receiver identity before matching durable ACK; phase advance only after its required ACK. Retry must preserve payload bytes, logical role, delivery ID and idempotency key, with only an explicitly recorded valid generation rebind. ACK replay is a no-op. Physical retransmission after an uncertain transport is not automatically a duplicate logical effect; unresolved effects without deduplication proof must BLOCK rather than claim exactly-once execution.
- READ-OLD/WRITE-CANONICAL: load representative old metadata, decision ledgers and missing-field forms using production readers. Inspect is byte-preserving. Apply preserves historical ledger prefixes and source artifacts and emits only canonical versioned Autoloop events/intents/ACKs/receipts. Maintain existing public wrappers. No retrospective identifier fabrication, legacy-format dual writing, ledger rewriting, or ambiguous-state guesses; contradictory, torn or insufficient old evidence becomes BLOCKED with references.
- Race recover/apply/resume/directive/review/ACK operations using explicit barriers. Count actual logical effects, commits and notification-sink events; preserve live ownership and typed stale-token outcomes. Reuse strong existing tests where they truly exercise these paths; strengthen inadequate coverage instead of replacing it with row counts.
- GREEN command: rtk proxy npm test -- src/__tests__/autoloop-recovery.test.ts src/__tests__/autoloop-outbox.test.ts src/__tests__/autoloop-secure-ledger.test.ts src/__tests__/autoloop-runner.test.ts src/__tests__/autoloop-dispatcher.test.ts src/__tests__/autoloop-trust-recovery-delivery.test.ts. Run affected SessionManager/kernel tests when touched. Commit each inseparable causal correction with its RED/GREEN references; fresh slice Reviewer findings return to the same Coder.

## Slice 3 — Public success, independent review and authentic adapters
- Production fence: src/{session-manager.ts,index.ts,embedded-server.ts,persistent-session.ts,persistent-codex-session.ts,persistent-agy-session.ts,persistent-cursor-session.ts,base-oneshot-session.ts}; src/autoloop/{planner-tools.ts,agent-tools.ts,dispatcher.ts,messages.ts,types.ts}. Adapter edits require demonstrated parser/lifecycle defects, not policy or model-routing changes.
- Test fence: src/__tests__/{autoloop-planner-tools.test.ts,autoloop-agent-tools.test.ts,autoloop-documentation-contract.test.ts,autoloop-durable-recovery-e2e.test.ts,agy-planner-e2e.test.ts,session-manager.test.ts,tool-registration.test.ts,embedded-server.test.ts,embedded-server-launcher.test.ts,persistent-session.test.ts,codex-session.test.ts,agy-session.test.ts,cursor-session.test.ts,autoloop-trust-recovery-boundaries.test.ts,autoloop-trust-recovery-adapters.test.ts}; common harness/fixtures. Documentation fence: skills/references/autoloop.md only.
- RED: exercise C/D/E/F through real public handlers and Planner controls, including persistence rejection, partial control batches, empty native completion, denial, eager recreation failure, invalid source/scope/idempotency fields and independent role startup. Observe terminal responses and actual effects; preserve wrapper input/output compatibility and bounded failures.
- Build four fresh protocol fixtures from current production parsers and existing authentic adapter tests: Codex thread.started/item.completed/turn.completed plus exec resume; Claude system/init, stream-json assistant/result and stdin turns; AGY init/conversation_id and nested result plus --conversation; Cursor system/session_id, assistant/result plus --resume. Record argv, protocol source locators and fixture provenance. Use SessionManager selection and actual adapter classes; no substituted send/parser/outbox/recovery implementation.
- Deterministic targets: Codex/gpt-6-astra, Claude/haiku through the existing alias resolver, AGY/gemini-3.8-flash-high, Cursor/auto. Capture requested and emitted model arguments. Cover create/nonempty reply, physical identity/continuity, empty/partial/protocol/process failures and available denial signals for each adapter; include Codex failure with exit zero, Claude denial/result handling, AGY empty and nonempty soft denials, and Cursor read-only enforcement.
- Compose each adapter with real Autoloop paths for Planner control persistence, Coder directive receipt, Reviewer-only request/ACK/verdict, both pre-ACK crash windows, deduplication, generation fencing, reset and recovery. Shared scenario code is permitted; each engine's invocation, fault, recipient bytes and durable artifacts must be independently observed. Use requirement IDs and trace references, not a mandated 96-row product or multiplied claims. Protocol-inapplicable subcases need reviewed reasons and cannot exempt an engine or a required core guarantee.
- GREEN commands: rtk proxy npm test -- src/__tests__/autoloop-trust-recovery-boundaries.test.ts src/__tests__/autoloop-trust-recovery-adapters.test.ts src/__tests__/autoloop-planner-tools.test.ts src/__tests__/autoloop-agent-tools.test.ts src/__tests__/agy-planner-e2e.test.ts src/__tests__/tool-registration.test.ts src/__tests__/embedded-server-launcher.test.ts src/__tests__/autoloop-documentation-contract.test.ts; additionally run the affected native adapter suites. Correct public reference claims to match measured behavior. Commit controls/adapter corrections and their tests atomically; obtain fresh read-only slice review.

## Final verification on one candidate
- Finish all implementation, tests, documentation, collector/validator and plan amendments before freeze. Commit atomically, then record C = full HEAD, T = its full Git tree, U, C0, exact diff inventories and an input manifest including source/tests/docs/scripts/package/lock/tsconfigs. Every final gate names C/T. Final logs remain in ignored A/evidence/C; do not create an evidence-only commit after freeze and thereby change the reviewed candidate.
- focused runs all Autoloop suites, incident/legacy/delivery/boundary/adapter additions, the four native adapter suites and required public tests once on C. Collector expands and records the exact discovered test-file list; missing selections or skipped required scenarios fail.
- Static commands: rtk proxy npm run format:check; rtk proxy npm run lint; rtk proxy npm run build. Explicitly check formatting of new scripts/fixtures and run validator tests. All must exit zero, with no script/threshold/config weakening.
- typecheck runs rtk proxy npm run typecheck:tests on U and C with the same installed compiler/dependencies. tsconfig.test.json identifies this as diagnostic. Retain both complete logs/exits and compare diagnostic multisets by file/code/message with audited location mapping; require zero added diagnostics. If U exits zero, C must too. Never suppress errors, loosen tsconfigs, or present a nonzero raw command as a raw PASS.
- full runs rtk proxy npm test. coverage runs rtk proxy npm run test:coverage with JSON coverage output retained. Require zero failing tests/unhandled errors and intact reports. Map changed critical release/identity/durability/recovery/control branches to executed tests and coverage observations; newly uncovered required critical behavior blocks completion. No scalar coverage target or denominator manipulation.
- e2e executes src/__tests__/autoloop-durable-recovery-e2e.test.ts and the authentic adapter integration suite in five consecutive complete fixture-only runs. concurrency executes the complete autoloop-outbox.test.ts, autoloop-dispatcher.test.ts and session-manager.test.ts suites plus the new barrier-driven race cases twenty consecutive times. Each run has separate logs and exact test counts; execute sequentially, use explicit synchronization and verify child cleanup. A failure stops that series, is diagnosed, and restarts it from 1 after correction; retain failed attempts.
- nonregression checks ordinary SessionManager lifecycle/persistence/TTL, MCP tool registration/result contracts, embedded server/async terminal outcomes, session-manager-workflow, fanout, council-postprocess, kernel and ultraapp integration tests, plus the full suite. Capture exact files from the existing repository inventory. Shared surfaces must preserve their existing contracts; Autoloop guarantees are attributed to another surface only where a direct test proves them.
- live runs only after deterministic gates pass. Use the built candidate's actual adapter classes, existing binaries/authentication and disposable read-only workspaces: one bounded first turn and one continuation per target above, sequentially. Capture binary path/version, source/build hashes, sanitized argv, permission flags, physical IDs, nonempty responses and continuity. Effective model is recorded only when exposed; otherwise use NOT_EXPOSED, never inference. These are adapter boundary smokes, not live proof of all recovery invariants.
- No live stress matrix, installation, permission relaxation, credential/configuration changes, runtime replacement, Gateway operation or customer work. Discovering a binary is not proof of usable capability. Missing model/auth/quota/runtime/isolation capability is BLOCKED and fails the required live gate; finish unaffected deterministic work, preserve evidence and report MATERIAL_BLOCKER if it remains unavailable.

## Success criteria — machine gates
- Commands G2–G20 are rtk proxy node scripts/autoloop-trust-recovery/verify.mjs <mode>. verify is read-only, recomputes predicates from raw evidence and exits nonzero for missing/failed/blocked proof. It does not launch expensive groups during inner loops. G1 is rtk proxy node --test scripts/autoloop-trust-recovery/verify.test.mjs. All gates are mandatory. All Coder-generated final gate artifacts are collected and validated under A/evidence/C and A/reports/; controller-owned ledger/verdict references under R are read-only inputs.
- [ ] G1 validator: negative and positive evidence-contract tests pass; Reviewer audits resistance to the rejected matrix's failure mode.
- [ ] G2 provenance: U/C0/C identities, source provenance, instruction/configuration hashes, ancestry, fences and protected evidence are intact.
- [ ] G3 incidents: all six U/C0/final comparisons have evidence-backed classifications and no unexplained NOT_PROVEN or final behavioral failure.
- [ ] G4 legacy: root cause demonstrated, clock/TTL cases and the competing generation-zero reservation/persistence regression pass.
- [ ] G5 durability: observed intent/send/ACK/replay/rebind/fencing/crash/resume behavior and READ-OLD/WRITE-CANONICAL contract pass.
- [ ] G6 controls: public reset/chat/recovery and independent Reviewer behavior satisfy C/D/E/F and compatibility contracts.
- [ ] G7 adapters: four authentic deterministic adapter integrations and their required fault observations pass.
- [ ] G8 focused: final focused inventory passes on C/T without required skips.
- [ ] G9 format: repository formatting and newly introduced verification files pass.
- [ ] G10 lint: unchanged lint command passes.
- [ ] G11 build: unchanged production build passes and its outputs bind to C/T.
- [ ] G12 typecheck: upstream diagnostic comparison proves no added test diagnostics.
- [ ] G13 full: complete repository test suite passes on C/T.
- [ ] G14 coverage: complete coverage execution and required critical-branch evidence pass.
- [ ] G15 e2e: five consecutive complete E2E/adapter-fixture runs pass.
- [ ] G16 concurrency: twenty consecutive complete concurrency runs pass.
- [ ] G17 nonregression: all named shared surfaces retain directly tested contracts.
- [ ] G18 live: all four minimal local adapter smokes have actual capability/continuity evidence; BLOCKED is not PASS.
- [ ] G19 review: fresh independent final read-only Reviewer advances exact U/C0/C/T and evidence-manifest hash with no unresolved material finding.
- [ ] G20 close: all prior gates remain current, local commits are atomic, protected state is intact, git diff --check succeeds, index/worktree are clean and no run-owned test children or pending corrective work remain.

## Reviewer rubric, commits and rollback
- For each slice, finish its focused gates and stop Coder edits before a fresh read-only review of that slice's base/head/tree and RED/GREEN evidence. For final review, G1–G18 must be green first. Review the full U..C implementation and C0..C corrections, raw evidence, six-incident matrix, legacy contract, shared callers and every deterministic gate; earlier reviews are advisory for later candidates.
- Final Reviewer independently reruns deterministic checks in isolated scratch without modifying candidate inputs; no extra live calls are required. The native controller records the verdict and Reviewer provenance bound to C/T and the evidence manifest. Coder cannot author the authoritative review record. Reject self-certification, fabricated historical logs, generic fixture relabeling, count-only tests, unobserved crashes, unsupported effective-model claims and narrative PASS.
- A material finding returns to the same Coder with a failing regression requirement. Correct it, commit a new candidate, rerun affected slice tests, repeat the complete final gate set on that new C/T and obtain another fresh final review. Never reuse a stale advance or mix final evidence from different candidates.
- Stage only named paths after inspecting the staged diff and rtk proxy git diff --cached --check. Separate harness preparation and each accepted causal correction into atomic local commits. Retain RED/GREEN logs and commit associations; never rewrite Tasks 1–8 or prior evidence. Final evidence stays append-only under A/evidence/C and A/reports/ outside the tracked candidate; the controller may hash/reference these artifacts and capture its verdict in its own ledger without advancing HEAD. Coder never writes the controller ledger or any other path under tasks/.
- Rollback: stop only owned test children, preserve all evidence, pause the same run and revert this run's identified correction commits in reverse dependency order using new commits after checking scope. Never reset-hard, apply/pop stashes, erase histories, touch unrelated worktrees or change the installed runtime. An uncommitted experiment is removed only after its exact patch/evidence is preserved and ownership verified.

## Stop conditions
- COMPLETE only when G1–G20 pass for one unchanged C/T and the final read-only Reviewer has no material findings. Report exact identities and evidence locations, including accepted historical exclusions and the limits of live proof.
- MATERIAL_BLOCKER only for a demonstrated unresolved correctness/evidence/capability/authority obstruction; give the failed gate, reproducer or command result, exact candidate, completed checks and the smallest condition required to resume. A red test with a viable correction is work to do, not a reason to stop prematurely.
- No scalar, plateau target or arbitrary repair-cycle cap; goal.json uses max_iters=null. Keep resource use proportional through focused slices, one Coder, sequential final repetitions and bounded live probes. Repeated failures without a new hypothesis trigger diagnosis in the same run. Temporary interruption preserves the run and evidence; only COMPLETE or MATERIAL_BLOCKER is a final outcome.
