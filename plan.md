# Plan — R2 Tasks 8–9 durable recovery
## Goal
Finish only documentation reconciliation and final four-engine validation on the integrated base. No scalar; acceptance is binary gates. This document is the single Tasks 8–9 reconciliation against bf52a981835f42d5c3bf4ff19c8cdfb7ef983625 (tree e7fd39ae18c3bdf630aeb82a3ba0b5c58f9b76cf).
## Authority and fixed state
Run: CLAWO-AUTOLOOP-DURABLE-RECOVERY-POST-UPSTREAM-TASK8-9-R2. In this plan R means tasks/CLAWO-AUTOLOOP-DURABLE-RECOVERY-POST-UPSTREAM-TASK8-9-R2.
Read R/planner-brief.md and its six named snapshots; snapshot SHA-256 values match R/checkpoints/r2-preflight.json. Both upstream 3e09b032a2f95fa520648f959f4ac9cdc7393350 and PR105 eee4fd3831d3855a5d7c98e39720dea12d52e760 are ancestors of the base. Planning inspection found a clean index/worktree.
Retain the immutable instructions and council snapshot. Use only its run-bound flat role engine/model fields: Planner codex/gpt-6-astra, Coder codex/gpt-5.6-terra, Reviewer codex/gpt-5.6-sol. Adapter test targets below are not worker bindings. No UAB, AgentBinding, governed envelopes, or effort overrides.
Tasks 1–7 remain closed: no merge-caused invalidation demonstrated by this inspection. Existing coverage is retained, not reimplemented. A newly failing requirement needs a deterministic reproduction and exact causal merge hunk before proposing any reopening; otherwise report a blocker for scope decision.
This turn authorizes only dedicated plan/goal controls and their artifact-only automatic commits. No workers or providers start. Execution waits for explicit spawn approval; no auto_proceed.
## Scope and paths
- Task 8 edit allowlist: skills/references/autoloop.md; .github/pull_request_template.md; src/__tests__/autoloop-documentation-contract.test.ts (new).
- README.md is excluded: it links the Autoloop reference rather than maintaining this API inventory.
- Task 9 edit allowlist: src/__tests__/autoloop-durable-recovery-e2e.test.ts; src/__tests__/autoloop-four-engine-matrix.test.ts (new); src/__tests__/fixtures/autoloop-four-engine/{codex,claude,agy,cursor}.mjs (four new files); src/__tests__/fixtures/autoloop-four-engine/README.md (protocol provenance); R/checks/verify.mjs; R/checks/live-boundary.ts; R/checks/verify.test.mjs; R/checkpoints/task8-9-evidence/**.
- The braces above enumerate four exact fixture paths, not a wildcard authorization. Existing tests may be read/run; edits outside this list require a concrete scoped amendment.
- Do not edit production src files, dependency/lock files, package scripts, tsconfigs, CI, council/configuration, credentials, runtime or Gateway. Preserve parent/R1, historical ledgers, stash, and retired dossier. No installation, Task 10, push, PR, publication, new logical successor, destructive cleanup, or external messaging.
- Disposable test files, build/coverage outputs and evidence stay project-local. Do not change user HOME/config; use project-local scratch paths through existing adapter/test options. If a required live boundary cannot respect these constraints, record BLOCKED.
## One-time reconciliation and Task 8
Stash 8ef468094d093d26b24b4e4c06902fd27cd8d3d1 was inspected as a diff against its first parent; only two files differ. Never apply/pop it wholesale.
- Reuse the PR-template optional durable-recovery checklist hunk: the existing template still supports it and its incident/root-cause/invariant/test/rollout/rollback/non-goals/risks fields remain valid.
- Reuse the reference hunk's recover/request_review inventory, eight effective phases, inspect/apply token examples, four recovery error/status rows, source-bound review identity, reset fencing, and conservative legacy compatibility, adapting insertion to current HEAD.
- Preserve upstream additions at skills/references/autoloop.md:55 and :170: AGY denial/empty-turn semantics and failure-atomic fenced-control batching. Do not overwrite them with stash-era text.
- Rewrite the stash's three-row incident map into the six original failure classes below; do not describe deterministic subprocess tests as live validation.
- Qualify uncertain delivery versus prepared recovery claims: replay unacknowledged outbox identity only when recoverable; unresolved prepared effects fail closed. Do not claim universal automatic retry or physical exactly-once execution.
- Expand missing independent spawn_coder/spawn_reviewer/request_review controls, strict success errors, reset postconditions and operator limits. Match src/autoloop/planner-tools.ts:430, src/index.ts:2127/:2230 and src/embedded-server.ts:1366; distinguish MCP completion from HTTP chat 202 queued and later terminal failure.
- Documentation contract test must check documented field requirements, error names/statuses, route examples and independent controls against actual registration/validation behavior. Test both valid and rejected examples; mere string presence is insufficient.
- Rollout text is inspect-first guidance only. Record manual-resolution conditions and local revert containment, without instructing this run to install or recover a real customer run.
## Task 9 deterministic adapter × role × invariant matrix
Create four distinct subprocess protocols using the actual production adapter classes, selected through SessionManager. Share scenario orchestration only; never relabel an AGY fake as another engine or mock out parser/transport success.
| Engine | Exact input model | Fixture protocol and argv contract |
| --- | --- | --- |
| Codex | gpt-5.6-sol | thread.started/thread_id, item.completed agent_message, turn.completed; exec then exec resume same thread; exact --model value (src/models.ts:221). |
| Claude | haiku | stream-json system/init session_id, assistant and result lifecycle; real alias resolver yields claude-haiku-4-5 (src/models.ts:168); no fallback-model or proxy substitution. |
| AGY | gemini-3.8-flash-high | init/conversation_id and nested result/status/response; --conversation on resume; preserve qualified --model exactly, no effort override. Supported qualified-slug contract: src/models.ts:347 and src/persistent-agy-session.ts:159. |
| Cursor | auto | actual Cursor system/session_id, assistant/result format; resume same session; captured argv contains adjacent --model auto without substitution (src/persistent-cursor-session.ts:133). |
Every engine has Planner, Coder and Reviewer cells. Define 12 role cells crossed with I1–I8 below (96 explicitly enumerated records); role-specific subassertions are explicit, not silently skipped.
- I1 all roles: create/start/first send; non-empty completion with physical identity; resume/recover preserves logical role; reset/recreate proves old release and newer generation.
- I2 all roles: empty/whitespace, invalid result, partial output, process error and applicable permission-denial signals. Planner additionally requires read-only handling, required-tool-denial rejection, validated/persisted controls and no phase advance on false success; distinguish native partial replies from strict Autoloop acceptance.
- I3 all roles: independent handoff path. Planner emits independent Coder control; Coder receives exactly intended directive; Reviewer receives source-bound request without Coder creation. Preserve joint-spawn compatibility.
- I4 Coder/Reviewer: durable intent before send, matching ACK before phase advance, retries/dedup and exact delivery_id/idempotency_key/payload SHA-256; reject mismatches. Planner cell proves origin/control persistence and phase gating; Planner is not an outbox recipient.
- I5 Coder/Reviewer: barrier-driven subprocess crash after intent before send, and after captured delivery before ACK; same identity on safe retry, acknowledged replay is no-op, uncertain prepared effect blocks. Planner cell observes both windows without inventing downstream success.
- I6 all roles: inspect is byte-for-byte read-only; token-bound apply, stale token rejection, repeated/concurrent apply/resume/replay produce one durable logical effect; no duplicate commits/notifications.
- I7 all roles: lease/generation fencing, stale work rejection, live/unknown-owner protection, absent-owner orphan release and concurrent release single winner.
- I8 all roles: legacy metadata/ledger compatibility, no historical rewrite; complete Coder checkpoint with all physical sessions gone reaches Reviewer-only verdict bound to original source SHA/iteration, zero Coder bridge and zero continuation run.
Use explicit IPC/file barriers, bounded waits and process cleanup assertions; no sleep-only correctness, network or real CLI discovery in fixture suites. Assert negative outcomes and durable bytes, not just exit status.
Adapter-specific cases include Codex protocol failure with exit zero; Claude result/partial/denial handling; AGY 1.1.26 empty soft denial and 1.2.2 non-empty denial, log sanitization and retained conversation; Cursor partial/empty results and enforced read-only permissions. Provenance README identifies corresponding existing adapter tests and protocol versions.
## Original failure → reproduction → cause → invariant → test → PASS evidence
Use these six rows in Task 8 docs and R/checkpoints/task8-9-evidence/failure-map.json; original reproductions are described in docs/superpowers/specs/2026-09-05-autoloop-durable-recovery-design.md:17. Do not fabricate historical command logs.
| Failure / prior reproduction | Root cause | Invariant and test mapping | Required PASS proof |
| --- | --- | --- | --- |
| Wrong directive replay after Planner confirmation | Volatile/current directive used instead of persisted identity | I3–I5; autoloop-outbox, autoloop-dispatcher, four-engine matrix | Captured recipient bytes/hash equal confirmed original, newer distractor never sent. |
| Expired activity lease leaves recoverable work paused | Physical liveness confused with durable phase | I6–I8; autoloop-recovery, session-manager, matrix | Artifacts reconstruct exact next action; live/unknown owner protected; repeat apply same receipt. |
| Dead session name blocks recreation | Reservation outlives physical owner | I1/I7; autoloop-dispatcher, durable E2E, matrix | Proved-absent owner released once, higher generation reuses name, stale owner rejected. |
| Reset reports success without replacement | Missing reset postcondition verification | I1/I7; session-manager, durable E2E, matrix | Release plus requested eager replacement proved; occupied/unknown state returns typed failure. |
| Empty chat succeeds without persisted control | Transport exit mistaken for logical success | I2; agy-planner-e2e, session-manager, embedded-server-launcher, matrix | Empty/missing session/denied required tool/malformed or missing control fail; no phase advance; async failure durable. |
| Existing checkpoint cannot reach Reviewer alone | Joint lifecycle couples Reviewer to Coder | I3/I8; autoloop-planner-tools, durable E2E, matrix | One source-bound verdict, no Coder start, no new logical run; retry adds no duplicate delivery. |
Each evidence row records exact test title, command, candidate identity, raw-output path/hash, prior reproduction locator, current result and assertion data. Historical causal explanation is distinct from fresh deterministic reproduction; all six need current PASS.
## Gates and evidence implementation
Coder creates R/checks/verify.mjs as a local evidence validator/runner with modes docs, matrix, reliability, typecheck, coverage, live, repository, freeze, review. Missing evidence is failure; never success by declaration. Add R/checks/verify.test.mjs with missing/stale/tampered evidence, duplicate/absent matrix cells, failed/reordered series and forged review rejection cases.
Every command log records start/end, exact argv/cwd, tool versions, exit code, candidate commit/source manifest and SHA-256. Evidence is sanitized and append-only by candidate directory; no credentials/private logs.
- G1 docs: documentation contract suite passes; Reviewer audits every retained/rewritten stash section and six failure rows.
- G2 matrix: all 96 records present exactly once with assertion evidence; all applicable assertions PASS. NOT_APPLICABLE requires per-subassertion adapter/role technical reason reviewed independently; it cannot exempt an engine, role, original failure, or required lifecycle guarantee.
- G3 reliability: fixture-only durable E2E plus four-engine matrix pass 5 consecutive complete runs. Entire autoloop-outbox.test.ts, autoloop-dispatcher.test.ts and session-manager.test.ts suites pass 20 consecutive complete runs. Retain every run's logs and counters; any flake stops the series, requires diagnosis/fix and restarts that affected series at 1. Never retry failed cases to hide a failed run.
- G4 static: rtk npm run format:check; rtk npm run lint; rtk npm run build all exit zero. No install. Run rtk npm run typecheck:tests on base and candidate with identical installed compiler/dependencies; tsconfig.test.json:13 makes this diagnostic, not upstream CI. Require no added diagnostics by file/code/message, map moved locations explicitly; keep both raw exits/logs. Baseline failure is not a candidate PASS without this comparison. Production tsc/build must remain green.
- G5 tests: focused docs/matrix/Autoloop/adapter tests, rtk npm test and rtk npm run test:coverage exit zero. Coverage validator checks report integrity and no newly uncovered critical changed recovery branch, with explicit branch/test mapping.
- G6 regressions: focused session-manager, tool-registration (MCP), embedded-server, embedded-server-launcher, session-manager-workflow, fanout, council-postprocess and kernel suites pass, plus full suite. Preserve semantics; never claim Fan-out/Council acquired Autoloop guarantees.
- G7 live: minimal four-engine boundary proof below is PASS on frozen candidate. BLOCKED or NOT_EXECUTED prevents Task 9 completion.
- G8 integrity/review: diff whitespace clean, index empty, implementation worktree clean, allowlist/ancestry/snapshot/stash checks pass; final independent read-only review advances exact frozen candidate with no material findings.
## Minimal authorized live boundary (execution only)
After all fixture/static/full gates pass, freeze implementation, docs, fixtures and check scripts. R/checks/live-boundary.ts uses the built candidate adapters, never an installed replacement. One sequential disposable scenario per engine under R/checkpoints/task8-9-evidence/scratch; no parallel provider runs or stress repetitions.
For each target capture resolved binary path/version, exact input model, sanitized argv, effective model when actually exposed (otherwise explicitly not_exposed), physical session IDs, non-empty first reply, resume-or-recover result and reset/recreate where supported. Record source/build manifest before and after.
Exercise Planner → independent Coder handoff → Reviewer delivery with exact identity/ACK, and Reviewer-only recovery on a disposable checkpoint; each engine participates through Reviewer. Use harmless bounded text/checkpoint prompts, not customer tasks. Model/version/auth/quota/binary unavailability is BLOCKED, never substituted. Do not infer effective model from requested model or label fixtures live.
Reuse existing credentials only through existing adapters; never print/copy secrets, install binaries, modify configuration, relax permissions, or restart Gateway. If adapter scratch/config behavior cannot remain project-local without changing runtime/config, stop that proof as BLOCKED. No native persistent user configuration writes are authorized.
## Freeze, commits, rollback and final review
Coder operates sequentially using TDD for test/harness corrections. Commit Task 8 docs/contracts atomically after focused verification; commit Task 9 fixture/evidence-check tooling atomically after its focused gates. Stage only explicit allowlisted paths; inspect staged diff and rtk git diff --cached --check before each local commit. Preserve history; no amend/rebase/reset-hard.
Record candidate C (full commit and tree) and SHA-256 manifest of source/docs/tests/check scripts/package/lock/tsconfig inputs. Run final gates/live against unchanged C, then store evidence in a separate atomic local commit E containing only R/checkpoints/task8-9-evidence files. Bind E back to C and prove candidate manifest unchanged; evidence cannot self-certify its own truth.
Stop Coder edits. Request one fresh independent read-only review through the run-bound Reviewer; older Cursor-review instructions are superseded by R2 snapshot, while Cursor remains a required adapter target. Review C plus E, six failure rows, all gates/raw logs, negative cases and security/concurrency/compatibility risks. Reviewer independently reruns deterministic gates with disposable outputs, makes no implementation edits and no extra live calls; returned verdict binds C/E/manifest.
The review control ledger records the verdict outside candidate source. Final read-only integrity check proves C/E unchanged. Any material finding returns to the same Coder for a scoped correction, new freeze and fresh affected/full verification, live evidence and final independent review; previous review is advisory.
Rollback means pause, preserve evidence/stash/snapshots, stop only disposable test children, and revert only this run's identified local change commits in reverse dependency order with new commits after scope check. Never discard user changes or historical state. Runtime rollback is unnecessary because this run never installs.
## Termination and Reviewer rubric
All G1–G8 must pass; scalar is null. max_iters is 100 as a runaway-loop safety boundary, not a success or quota target; reaching it holds the same run for diagnosis. Two repeated failures without a new hypothesis pause for diagnosis. Missing capability or scope conflict yields one precise BLOCKED reason; preserve same R2, no successor.
Reject fabricated historical reproductions, generic-fixture relabeling, hidden fallbacks, self-declared PASS, skipped cells, stale candidate evidence, relaxed assertions, unchanged claimed-effective-model guesses, suppressed typecheck diagnostics, rewrites of append-only evidence and claims beyond Autoloop. Completion requires current verified evidence and independent advance.
